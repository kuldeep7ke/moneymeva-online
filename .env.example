# Runtime config is NOT read from env vars anymore.
# All values (Supabase URL/key, jsonbin Bin IDs) live obfuscated (XOR + base64) in:
#   src/lib/env.ts
# Edit that file and rebuild to point the app at a different Supabase project
# or different jsonbin bins. Nothing here is required — this file is inert.

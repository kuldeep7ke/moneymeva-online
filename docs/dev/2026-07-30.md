# 2026-07-30 — Dev Log

> Global toast, skeleton loading, empty state upgrades

---

## Done

- Created `Toast.tsx` — global toast system with success/error/warning/info
- Created `Skeleton.tsx` — reusable skeleton components (Card, Table, Chart, List)
- Wired `ToastProvider` into root layout
- Replaced all 6 `alert()` calls with toast:
  - `adjustments/page.tsx` — invalid amount warning
  - `developer/page.tsx` — clear data error
  - `settings/page.tsx` — 4 instances (backup import, CSV import, user switch)
- Upgraded ledger loading from spinner to skeleton rows
- Upgraded 12 empty states with icons + headings + CTAs:
  - Dashboard: chart, expenses, recent, tasks, recurring, goals
  - TransactionPage: mobile, desktop, archive
- Built Obsidian vault in `docs/`
- Version bumped to v7.1.1.28

## Blocked

- Nothing

## Notes

- `useToast()` returns `addToast` function directly (not context object)
- Toast uses existing `slide-up` CSS animation
- Skeleton uses Tailwind `animate-pulse`

## Next

- Test toast in browser
- Add more skeleton loading to other pages
- Polish empty state CTAs

---

**Version:** v7.1.1.28
**Commit:** aec1a26

---

#money-meva #dev-log
